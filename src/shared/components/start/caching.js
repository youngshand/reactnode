import React from 'react'; // react must be imported for stateless function

export default function StartCaching() {
  return (
    <div>
      <h3>Connect to an api</h3>
      <p>Help about how to setup an api connection...</p>
    </div>
  );
}
